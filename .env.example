# =============================================================================
# COBALT API - ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# =============================================================================
# Copie este arquivo para .env e configure as variáveis conforme necessário
# Para EasyPanel, use ${VARIAVEL} no docker-compose.yml

# =============================================================================
# CONFIGURAÇÕES BÁSICAS DA API
# =============================================================================

# URL da API (obrigatório)
API_URL=https://seu-dominio.com

# Nome da instância (opcional)
API_NAME=cobalt

# Porta da API (padrão: 9000)
API_PORT=9000

# Porta para túnel (opcional)
TUNNEL_PORT=

# =============================================================================
# CONFIGURAÇÕES DE CORS
# =============================================================================

# Domínios permitidos para CORS (separados por vírgula)
CORS_WILDCARD=*
CORS_URL=https://seu-frontend.com

# =============================================================================
# CONFIGURAÇÕES DE RATE LIMITING
# =============================================================================

# Limite de requisições por minuto (padrão: 20)
RATE_LIMIT=20

# Janela de tempo para rate limit em minutos (padrão: 1)
RATE_LIMIT_WINDOW=1

# =============================================================================
# CONFIGURAÇÕES DE DURAÇÃO E TAMANHO
# =============================================================================

# Duração máxima de vídeo em minutos (padrão: 10800 = 3 horas)
DURATION_LIMIT=10800

# Tamanho máximo de arquivo em MB (padrão: 128)
FILE_SIZE_LIMIT=128

# =============================================================================
# CONFIGURAÇÕES DE SESSÃO
# =============================================================================

# Tempo de vida da sessão em minutos (padrão: 21600 = 6 horas)
SESSION_LIFETIME=21600

# =============================================================================
# CONFIGURAÇÕES DE PROCESSAMENTO
# =============================================================================

# Prioridade de processamento (padrão: 10)
PROCESSING_PRIORITY=10

# Forçar processamento local (never, session, always)
FORCE_LOCAL_PROCESSING=never

# =============================================================================
# CONFIGURAÇÕES DE PROXY EXTERNO
# =============================================================================

# Proxy HTTP (opcional)
HTTP_PROXY=

# Proxy HTTPS (opcional)
HTTPS_PROXY=

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================

# Chave secreta do Turnstile (Cloudflare)
TURNSTILE_SECRET=

# Chave do site do Turnstile (Cloudflare)
TURNSTILE_SITE_KEY=

# Segredo JWT (mínimo 32 caracteres)
JWT_SECRET=seu-jwt-secret-muito-seguro-com-pelo-menos-32-caracteres

# =============================================================================
# CONFIGURAÇÕES DE CLUSTERING E REDIS
# =============================================================================

# Número de instâncias (padrão: 1)
# Se > 1, Redis é obrigatório
INSTANCE_COUNT=1

# URL do Redis (obrigatório se INSTANCE_COUNT > 1)
# Formato: redis://usuario:senha@host:porta/db
REDIS_URL=redis://redis:6379

# =============================================================================
# CONFIGURAÇÕES DE SERVIÇOS
# =============================================================================

# YouTube - Configurações
YOUTUBE_HLS=true
YOUTUBE_CODEC=h264

# Desabilitar serviços específicos (separados por vírgula)
# Exemplo: youtube,twitter,tiktok
DISABLED_SERVICES=

# =============================================================================
# CONFIGURAÇÕES AVANÇADAS
# =============================================================================

# Modo de desenvolvimento (true/false)
DEV_MODE=false

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Timeout para requisições em segundos (padrão: 30)
REQUEST_TIMEOUT=30

# =============================================================================
# CONFIGURAÇÕES ESPECÍFICAS DO EASYPANEL
# =============================================================================

# Domínio principal (usado pelo EasyPanel)
DOMAIN=seu-dominio.com

# Subdomínio da API (usado pelo EasyPanel)
API_SUBDOMAIN=api

# Certificado SSL automático (true/false)
SSL_AUTO=true

# =============================================================================
# CONFIGURAÇÕES DE COOKIES (OPCIONAL)
# =============================================================================

# Caminho para arquivo de cookies do YouTube (opcional)
# Usado para contornar limitações de idade/região
YOUTUBE_COOKIES_PATH=/app/cookies.json

# =============================================================================
# CONFIGURAÇÕES DE WATCHTOWER (ATUALIZAÇÕES AUTOMÁTICAS)
# =============================================================================

# Intervalo de verificação em segundos (padrão: 3600 = 1 hora)
WATCHTOWER_POLL_INTERVAL=3600

# Limpar imagens antigas após atualização (true/false)
WATCHTOWER_CLEANUP=true

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
#
# 1. API_URL é obrigatório e deve ser a URL completa da sua API
# 2. Se INSTANCE_COUNT > 1, REDIS_URL é obrigatório
# 3. JWT_SECRET deve ter pelo menos 32 caracteres
# 4. Para produção, sempre configure CORS_URL com seu domínio específico
# 5. Para usar cookies do YouTube, monte o arquivo em /app/cookies.json
# 6. Turnstile é opcional mas recomendado para proteção contra spam
# 7. No EasyPanel, use ${NOME_DA_VARIAVEL} no docker-compose.yml
#
# =============================================================================